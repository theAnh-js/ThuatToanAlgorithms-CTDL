package HashTable;

import javax.tools.ForwardingJavaFileManager;

public class UngDungCuaHashTable {

    // Hash Table có 2 ứng dụng chính:
    // 1. HashSet: thường để quản lý 1 tập các keys.
    // (Khi chúng ta chỉ cần quản lý khóa/key)
    // Bài toán cơ bản nhất: Kiểm tra xem phần tử(thông qua key)
    // đã tồn tại hay chưa.

    // 2.HashMap: thường để quản lý 1 tập các cặp key-value
    // (Khi chúng ta cần quản lý 1 thông tin nào đó dựa vào khóa/key)
    // Bài toán cơ bản nhất: Đếm số lượng xuất hiện của các phần tử.
}
